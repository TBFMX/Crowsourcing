<%= form_for(@amend) do |f| %>
  <% if @amend.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@amend.errors.count, "error") %> prohibited this amend from being saved:</h2>

      <ul>
      <% @amend.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.hidden_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  <div class="field">
    <%= f.label :project_id %><br>
    <%= f.hidden_field :project_id %>
  </div>
  <div class="field">
    <%= f.label :image_id %><br>
    <%= f.file_field :image_id %>
  </div>
  <div class="actions">
    <%= f.submit nil, class: "boton" %>
  </div>
<% end %>
